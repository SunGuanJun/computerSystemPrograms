#include <stdio.h>
#include <stdlib.h>

void main()
{
    /**
    int a = 10;
    show_int(a);
    show_float(2.0f);

    void *b = &a;
    show_pointer(b);
    */

    //show_ascii();

    show_chars("abcdef");

}
